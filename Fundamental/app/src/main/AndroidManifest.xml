<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.dicoding.fundamental">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.Fundamental">
    <activity
      android:name=".fragment.MainFragmentActivity"
      android:exported="false" />
    <activity android:name=".VolumeActivity" />
    <activity android:name=".IntentResultActivity" />
    <activity android:name=".IntentDialActivity" />
    <activity android:name=".IntentWithObjectActivity" />
    <activity android:name=".IntentWithoutValueActivity" />
    <activity
      android:name=".MainMenuActivity"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>

</manifest>